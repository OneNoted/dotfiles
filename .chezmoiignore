# Repo-only source support files should not be installed into $HOME.
*.md
bootstrap
bootstrap/**
docs
docs/**
justfile

{{ if ne .chezmoi.hostname "aeolus" }}
~/.config/hypr/monitors.conf
.config/systemd/user/t3code.service
{{- end }}

{{ if eq .chezmoi.hostname "hephaestus" }}
~/.config/fish/completions/paru.fish
{{- end }}

# ~/.config/nvim is synced by the nvim profile switcher, not directly by chezmoi.
.config/nvim
.config/nvim/**
~/.config/nvim
~/.config/nvim/**

{{- if eq .chezmoi.hostname "nixos" }}
# Ignore all by default if on NixOS
**
# Unignore following (why do I even bother making comments at this point)
!.config/
!.config/fish/
!.config/fish/**
!.config/shared/
!.config/shared/**
!.config/starship.toml
!.config/yazi/**
!.config/fastfetch/**
!.config/atuin/
!.config/atuin/**
!.config/television/**
!.config/zellij/**
{{- end }}
